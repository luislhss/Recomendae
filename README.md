#How to install and deploy our application.

You need have previous installed the Docker. Then run the command `docker compose --env-file ./config/env.dev up` to startup a dev version of the application.